<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Individual Schedules</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
     <!-- header -->
     <%- include('../partials/header') %>
    <!-- main -->
    <main>  
        <br><br>
        <div class="infotable">
            <h1 class="heading">Schedules for user_id:<%= userid %></h1>
        <table>
            <thead>
                <tr>
                    <th>User Id</th>
                    <th>Day</th>
                    <th>Start At</th>
                    <th>End At</th>
                </tr> 
            </thead>
            <tbody>
                <% for(let i=0;i<schedule.length;i++){ %>
                    <tr>
                        <td data-label="user_id"><%= schedule[i].user_id %></td>
                        <td data-label="day"><%= schedule[i].day %></td>
                        <td data-label="start_at"><%= schedule[i].start_at %></td>
                        <td data-label="end_at"><%= schedule[i].end_at %></td>
                    </tr>
                    <% } %>
                
            </tbody>
        </table>
        </div>
        
    </main>
    <!-- footer -->
    <%- include('../partials/footer') %>
</body>
</html>