<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Individual Schedules</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
     <!-- header -->

    <!-- main -->
    <main>  
        <table>
            <thead>
                <tr>
                    <th>User Id</th>
                    <th>Day</th>
                    <th>Start At</th>
                    <th>End At</th>
                </tr> 
            </thead>
            <tbody>
                <% for(let i=0;i<schedule.length;i++){ %>
                    <tr>
                        <td><%= schedule[i].user_id %></td>
                        <td><%= schedule[i].day %></td>
                        <td><%= schedule[i].start_at %></td>
                        <td><%= schedule[i].end_at %></td>
                    </tr>
                    <% } %>
                
            </tbody>
        </table>
    </main>
    <!-- footer -->
</body>
</html>